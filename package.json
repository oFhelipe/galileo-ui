{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build-storybook": "cd packages/docs && npm run build"
  },
  "packageManager": "npm@10.2.3",
  "dependencies": {
    "turbo": "^2.3.3"
  }
}
